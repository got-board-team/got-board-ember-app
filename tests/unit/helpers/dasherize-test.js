import { dasherize } from 'got-board-game/helpers/dasherize';
import { module, test } from 'qunit';

module('Unit | Helper | dasherize');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dasherize([42]);
  assert.ok(result);
});
