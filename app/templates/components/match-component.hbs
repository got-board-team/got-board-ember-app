{{tracks-component match=match}}
{{board-component board=board}}

{{#if isPlayer}}
  {{player-dock-component player=currentPlayer}}
  {{#dock-component id="board-dock" side="right" }}
  <div class="garrison-winterfell" draggable="true"
    >
  </div>
  {{/dock-component}}
{{else}}
  {{join-match-component match=match}}
{{/if}}
